## 1. Inventory Completed Change Capabilities

- [x] 1.1 Identify completed changes whose delivered capabilities are still active and should be canonicalized
- [x] 1.2 Enumerate the capability spec names that will be bootstrapped into `openspec/specs/`

## 2. Create Canonical Main Specs

- [x] 2.1 Create canonical spec files under `openspec/specs/` for active capabilities from completed changes
- [x] 2.2 Add provenance and lifecycle metadata to each canonical spec
- [x] 2.3 Create `openspec/specs/INDEX.md` listing canonical specs, status, and source change

## 3. Verify And Finalize

- [x] 3.1 Verify `openspec/specs/` now contains the expected canonical spec files
- [x] 3.2 Run `openspec validate --strict`
- [x] 3.3 Archive this bootstrap change after validation passes and tasks are complete
