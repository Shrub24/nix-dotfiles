## 1. Retire local packages

- [ ] 1.1 Remove the `iii-engine` and `agentmemory` derivations and their flake overlay/source wiring.
  - refs: `pkgs/iii-engine/`, `pkgs/agentmemory/`, `flake.nix`, `modules/home/nix.nix`
  - criteria: neither package is built, overlaid, or allowlisted as unfree

## 2. Remove inactive runtime integration

- [ ] 2.1 Remove Agentmemory's Home Manager module, its group import, the dormant host configuration, and Hermes integration.
  - refs: `modules/home/agents/agentmemory.nix`, `modules/home/agents/default.nix`, `hosts/arch/home.nix`, `modules/home/agents/hermes.nix`
  - criteria: no Agentmemory options, service, plugin, or memory-provider wiring remains
- [ ] 2.2 Remove Agentmemory from the web-service catalog and stale source metadata comments.
  - refs: `lib/web-services.nix`, `nvfetcher.toml`
  - criteria: no current service catalog exposes Agentmemory

## 3. Reconcile documentation

- [ ] 3.1 Update maintained documentation to remove Agentmemory and III Engine from current architecture, structure, package, service, and secret descriptions.
  - refs: `README.md`, `ARCHITECTURE.md`, `STRUCTURE.md`, `docs/web-services-catalog.md`
  - criteria: maintained docs describe only active components and do not retain stale Agentmemory secret/service claims
## 4. Validate removal

- [ ] 4.1 Run an exhaustive active-reference check and Nix formatting for changed Nix files.
  - criteria: active configuration and maintained docs contain no Agentmemory or III Engine references; historical archives and generated ByteRover cache are excluded
- [ ] 4.2 Evaluate the flake and Home Manager activation package.
  - verify: `nix flake check .` and `nix build .#homeConfigurations.saurabhj.activationPackage`
