diff --git a/litellm/litellm_core_utils/streaming_handler.py b/litellm/litellm_core_utils/streaming_handler.py
index 1111111..2222222 100644
--- a/litellm/litellm_core_utils/streaming_handler.py
+++ b/litellm/litellm_core_utils/streaming_handler.py
@@ -294,6 +294,12 @@
         if len(self.chunks) < 2:
             return
 
+        # Some providers (e.g. vLLM-backed APIs) emit metadata-only stream chunks
+        # where `choices` is empty. Those chunks cannot contribute to a repetition
+        # signal, so skip them instead of IndexError-ing.
+        if not self.chunks[-1].choices or not self.chunks[-2].choices:
+            return
+
         last_content = self.chunks[-1].choices[0].delta.content
